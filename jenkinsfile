pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                bat "build.bat ${pipelineParams.buildBatArgument}"
            }
        }
        stage('test') {
            steps {
                echo 'testing'
            }
        }
    }
}